"use client";
import { motion, useReducedMotion } from "framer-motion";
import { Reveal } from "./Reveal";
const rows=[['Manual hours / week identified','37'],['Workflows flagged for automation','6'],['Highest-leverage process','Intake triage'],['Estimated time-to-launch','27 days'],['Status','Ready for implementation']];
export function Readout(){const reduce=useReducedMotion();return <section className="bg-panel"><div className="wrap"><Reveal><div className="section-head"><p className="eyebrow">Sample Outcome</p><h2>What an assessment turns up</h2><p>An illustrative readout from a typical engagement — the shape of the result, not a specific client&apos;s numbers.</p></div></Reveal><Reveal className="overflow-hidden rounded border border-white/15 bg-[#131b30]"><div className="flex items-center gap-2 border-b border-white/8 px-5 py-3.5 font-mono text-xs text-faint"><span className="size-2 rounded-full bg-faint"/>assessment_output — ops_workflow_audit.log</div><div className="p-6 font-mono text-sm md:px-9 md:pb-9">{rows.map(([label,value],i)=><motion.div className="flex flex-col justify-between gap-1 border-b border-white/8 py-3.5 last:border-0 sm:flex-row" key={label} initial={reduce?false:{opacity:0,y:6}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{duration:.5,delay:i*.14}}><span className="text-muted">{label}</span><span className={i>1?'text-blue':'text-[#eef0f3]'}>{value}</span></motion.div>)}</div></Reveal></div></section>}
