import { Reveal } from "./Reveal";
const services=[['01','AI Readiness Assessment','A structured audit of your operations to identify the two or three places AI will pay for itself fastest — with a plan, not just a report.'],['02','Automation Implementation','Hands-on build of the agents, integrations, and workflows identified in assessment — deployed into your existing tools, not a parallel system.'],['03','Advisory & Change Management','Ongoing guidance for leadership and teams as AI changes how work gets done, so the system survives past the initial rollout.'],['04','Impact Measurement','Clear before/after metrics tied to the business outcome the project was meant to move — time saved, error rate, throughput, cost.']];
export function Services(){return <section id="services"><div className="wrap"><Reveal><div className="section-head"><p className="eyebrow">Services</p><h2>Where we typically start</h2><p>Most engagements begin in one of these four places, depending on how much groundwork is already done.</p></div></Reveal><Reveal className="grid md:grid-cols-2">{services.map(([num,title,body])=><article className="border border-white/8 p-10 transition hover:border-white/15 hover:bg-white/[.015]" key={num}><span className="eyebrow mb-3.5 block">{num}</span><h3 className="mb-2.5 font-heading text-xl">{title}</h3><p className="text-[.94rem] text-muted">{body}</p></article>)}</Reveal></div></section>}
