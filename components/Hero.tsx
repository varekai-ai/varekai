"use client";
import Image from "next/image";
import { motion, useReducedMotion } from "framer-motion";
export function Hero() { const reduce = useReducedMotion(); return <><section id="top" className="relative flex min-h-screen items-center overflow-hidden pt-[76px]"><motion.div className="absolute -right-[8%] top-1/2 w-[640px] max-w-[60vw] -translate-y-1/2" initial={reduce ? false : {opacity:0, scale:.94}} animate={{opacity:1,scale:1}} transition={{duration:1}}><Image src="/hero-mark.svg" alt="" width={640} height={640} priority /></motion.div><motion.div className="wrap relative z-10 w-full" initial={reduce ? false : {opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{duration:.7}}><div className="max-w-[640px]"><p className="font-mono text-[.82rem] uppercase tracking-[.14em] text-[#9aa0a8]">Automation. Instantly.</p><h1 className="my-5 font-heading text-[clamp(2.4rem,5.5vw,4rem)] font-semibold leading-[1.05]">AI automation that&apos;s <span className="text-gradient">live in weeks</span>, not quarters.</h1><p className="mb-9 max-w-[480px] text-[1.08rem] text-muted">Varekai builds automation directly into how your team already works — scoped, built, and running in production in under a month.</p><div className="flex flex-wrap items-center gap-4"><a className="button" href="#contact">Start an assessment</a><a className="border-b border-white/15 pb-0.5 text-sm text-muted hover:border-blue hover:text-white" href="#approach">See how it works</a></div></div></motion.div></section><ScanDivider /></> }
function ScanDivider(){ const reduce=useReducedMotion(); return <div className="flex h-[90px] items-center justify-center px-8" aria-hidden="true"><svg className="max-w-[1180px]" viewBox="0 0 1180 40" preserveAspectRatio="none"><defs><linearGradient id="line" x1="0" x2="1"><stop stopColor="#1e6fb8" stopOpacity="0"/><stop offset=".5" stopColor="#5bb0e6" stopOpacity=".9"/><stop offset="1" stopColor="#1e6fb8" stopOpacity="0"/></linearGradient></defs><motion.line x1="0" y1="20" x2="1180" y2="20" stroke="url(#line)" strokeWidth="1.5" initial={reduce ? false : {pathLength:0}} whileInView={{pathLength:1}} viewport={{once:true,amount:.5}} transition={{duration:1.4,ease:"easeInOut"}} /></svg></div> }
