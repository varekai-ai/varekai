import { Reveal } from "./Reveal";
const stages=[['WEEK 1','Assess & scope','We map your workflows as they really run and scope the single automation with the fastest, clearest payoff — not the biggest, the fastest.'],['WEEKS 2–3','Build & integrate','We build directly into the tools your team already uses, so there’s no separate system to adopt and no workflow to relearn.'],['WEEK 4','Launch & measure','Live in production, with the metric it was built to move tracked from day one — so you know it’s working, not just running.']];
export function Approach(){return <section className="bg-panel" id="approach"><div className="wrap"><Reveal><div className="section-head"><p className="eyebrow">The Approach</p><h2>From first call to live automation in under a month.</h2><p>Most automation projects stall in planning. Ours don&apos;t — here&apos;s the timeline we hold ourselves to on a typical engagement.</p></div></Reveal><Reveal className="grid border border-white/8 bg-white/8 md:grid-cols-3">{stages.map(([num,title,body])=><article className="stage bg-panel p-9 lg:p-11" key={num}><p className="font-mono text-xs tracking-widest text-faint">{num}</p><h3 className="my-4 font-heading text-[1.4rem] text-[#eef0f3]">{title}</h3><p className="text-[.95rem] text-muted">{body}</p></article>)}</Reveal></div></section>}
